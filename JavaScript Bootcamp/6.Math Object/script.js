const width = +prompt("Enter the Width of rectangle = ")
const height = +prompt("Enter the Height of rectangle = ")

// const areaOfRectangle = (width * height)

console.log("Area of Rectangle =" ,(width * height));
document.write(`Area of Rectangle = ${width * height}`)// tamplet laterals
